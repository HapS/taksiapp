<!-- Global Context Örneği -->
{% if global %}
    <h6 class="fw-bold mt-4 mb-2">
        <i class="bi bi-globe me-2"></i>Global İçerikler
    </h6>
    <ul class="list-unstyled">
        {% for key, content in global %}
            {% set lang_data = content.data.langs[current_language] %}
            {% if lang_data %}
                <li class="mb-1">
                    <a href="/{{ current_language }}/{{ content.content_type }}/{{ lang_data.slug }}-{{ content.id }}" class="text-body-secondary text-decoration-none small">
                        <i class="bi bi-arrow-right me-1"></i>{{ lang_data.title }}
                    </a>
                </li>
            {% endif %}
        {% endfor %}
    </ul>
{% endif %}



{% if global %}
    <ul class="list-unstyled">
        {% for content in global.gcx_84.data.sub_contents %}
        
                <li class="mb-1">
                    <a href="/{{ current_language }}/-{{ content.id }}" class="text-body-secondary text-decoration-none small">
                        <i class="bi bi-arrow-right me-1"></i>{{ content.data.langs[current_language].title }}
                    </a>
                </li>
    
        {% endfor %}
    </ul>
{% endif %}